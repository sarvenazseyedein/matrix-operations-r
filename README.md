 Matrix Operations in R

This project demonstrates basic matrix operations in R.

The script:
- creates a 3x3 matrix
- computes the transpose
- calculates the determinant
- computes the inverse matrix
- verifies the inverse using matrix multiplication

Package used:
matlib
